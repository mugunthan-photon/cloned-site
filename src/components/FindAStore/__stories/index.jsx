import React from 'react';
import FindAStore from '../../FindAStore/FindAStore';

const stories = [{
    name: 'Find a store',
    story: () => (
        <div>
            <FindAStore />
        </div>
      ),
}];

export default stories;
