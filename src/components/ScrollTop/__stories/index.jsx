import React from 'react';
import ScrollTop from '../ScrollTop';

const stories = [
    {
        name: 'ScrollTop',
        story: () => (
            <div>
                <ScrollTop />
            </div>
       ),
    },
];
export default stories;
